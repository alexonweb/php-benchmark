# PHP file write, read, delete speed benchmark

Simple file write speed test. Measures the speed of writing, reading and deleting **JSON** and **YAML** files. You can test hosting or your own server.

`cli.php` - Command line version

